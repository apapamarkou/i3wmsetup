#!/bin/bash

# Default system monitor is confirued in .profile
#xfce4-terminal --hide-scrollbar --hide-menubar --geometry=80x24 --title="Network Configuration" -x $NETWORK_CONFIG
alacritty --title="Network Configuration" -o window.dimensions.columns=80 -o window.dimensions.lines=24 -e $NETWORK_CONFIG
